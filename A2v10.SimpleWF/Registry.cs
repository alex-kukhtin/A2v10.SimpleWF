using System;
using System.Collections.Generic;
using System.Text;

namespace A2v10.SimpleWF
{

	public static class Registry
	{
		/*
		private static Dictionary<String, Action<RunContext, Instruction>> _map = new Dictionary<String, Action<RunContext, Instruction>>();

		public static void Register(String name, Action<RunContext, Instruction> run)
		{
			_map.Add(name, run);
		}

		public static Action<RunContext, Instruction> GetActivity(String name)
		{
			return _map[name];
		}
		*/
	}
}
